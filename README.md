# mantle-live-wire
An onchain news desk for Mantle. Reads chain TVL, protocol movers, stablecoin supply and prices live, then writes the rundown itself. Broadcast UI, no API key, runs in your browser. Track 2, Mantle Research Challenge.
